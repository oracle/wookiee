package com.oracle.infy.wookiee

import utest._

object TestExample extends TestSuite {

  val tests = Tests {
    test("put your wookiee tests here") {
      assert(true)
    }
    test("this is UTest test, but you can put ScalaTest classes in this directory") {
      assert(true)
    }
  }
}
