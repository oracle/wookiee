# TODO: This is the file where you component's custom settings go.
wookie-${component-name.toLowerCase()} {
  manager = "com.webtrends.harness.component.${component-name.toLowerCase()}.${component-name}Manager"
  enabled = true
  dynamic-component = true
}